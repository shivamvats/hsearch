#include <hsearch/search/dijkstra.h>

namespace hsearch {
    Dijkstra::Dijkstra( LatticePlanningSpacePtr& pspace_ptr_  ) : LatticePlanner( pspace_ptr_ ){}

    bool Dijkstra::isGoal( NodeId ){

    }
}
